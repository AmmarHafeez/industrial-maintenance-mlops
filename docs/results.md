# Results

No benchmark metrics are committed with the repository.

Training runs write local metrics to:

```text
reports/metrics/training_metrics.json
```

The metrics file is generated from the configured local dataset, train-validation split, window settings, RUL clipping value, model configuration, and random seed. Generated metrics and benchmark outputs are ignored by Git.
